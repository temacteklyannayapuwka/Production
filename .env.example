# Django
DJANGO_SECRET_KEY=replace-with-a-long-random-secret
DJANGO_DEBUG=false
DJANGO_ALLOWED_HOSTS=stavplus.ru,www.stavplus.ru,localhost,127.0.0.1

# The existing production site uses SQLite by default (db.sqlite3).
# To switch to PostgreSQL, uncomment and fill in every value below.
# DB_ENGINE=django.db.backends.postgresql_psycopg2
# DB_NAME=your_database_name
# DB_USER=your_database_user
# DB_PASSWORD=replace-with-the-database-password
# DB_HOST=localhost
# DB_PORT=5434

# PostgreSQL example
# Keep these commented out while using SQLite.
